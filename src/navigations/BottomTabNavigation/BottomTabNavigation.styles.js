import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBarStyle: {
    backgroundColor: "#171717",    // Fondo gris oscuro para el tab bar
    borderTopWidth: 0,             // Elimina la línea superior
    height: 60,                    // Altura del tab bar
    paddingBottom: 8,              // Espacio inferior
    paddingTop: 8,                 // Espacio superior
  },
});